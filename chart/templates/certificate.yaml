{{- if and .Values.webhook.enabled .Values.certManager.enabled }}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "janus.fullname" . }}-selfsigned
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "janus.labels" . | nindent 4 }}
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "janus.certName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "janus.labels" . | nindent 4 }}
spec:
  dnsNames:
    - {{ include "janus.webhookServiceName" . }}.{{ .Release.Namespace }}.svc
    - {{ include "janus.webhookServiceName" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: {{ include "janus.fullname" . }}-selfsigned
  secretName: {{ include "janus.certName" . }}
{{- end }}
