---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: backups.backup.janus.io
spec:
  group: backup.janus.io
  names:
    kind: Backup
    listKind: BackupList
    plural: backups
    singular: backup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.targetName
      name: Target
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Backup is a request to create a consistent backup of a target resource
          using the semantics defined in a BackupContract.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupSpec defines a request to back up a target resource.
            properties:
              contractRef:
                description: contractRef is the name of the BackupContract that defines
                  backup semantics.
                type: string
              targetName:
                description: targetName is the name of the target resource to back
                  up.
                type: string
              targetNamespace:
                description: |-
                  targetNamespace is the namespace of the target resource.
                  If not specified, defaults to the Backup's namespace.
                type: string
            required:
            - contractRef
            - targetName
            type: object
          status:
            description: BackupStatus defines the observed state of a Backup.
            properties:
              artifacts:
                description: artifacts lists the resources and volume snapshots captured
                  during backup.
                items:
                  description: BackupArtifact records a single resource or volume
                    snapshot captured during backup.
                  properties:
                    apiGroup:
                      description: apiGroup is the API group of the captured resource.
                      type: string
                    dataRef:
                      description: |-
                        dataRef is a reference to where the resource data is stored.
                        For Kubernetes resources, this is the name of the ConfigMap holding the serialized YAML.
                        For PVCs, this is the name of the VolumeSnapshot.
                      type: string
                    kind:
                      description: kind is the kind of the captured resource.
                      type: string
                    name:
                      description: name is the name of the captured resource.
                      type: string
                    namespace:
                      description: namespace is the namespace of the captured resource.
                      type: string
                  required:
                  - dataRef
                  - kind
                  - name
                  type: object
                type: array
              completedAt:
                description: completedAt is the time the backup operation completed.
                format: date-time
                type: string
              conditions:
                description: conditions represent the current state of the Backup.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: message provides human-readable details about the current
                  phase or failure.
                type: string
              phase:
                description: phase is the current phase of the backup operation.
                enum:
                - Pending
                - Quiescing
                - Snapshotting
                - Resuming
                - Completed
                - Failed
                type: string
              startedAt:
                description: startedAt is the time the backup operation started.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
