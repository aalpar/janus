---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: resourcechanges.tx.janus.io
spec:
  group: tx.janus.io
  names:
    kind: ResourceChange
    listKind: ResourceChangeList
    plural: resourcechanges
    singular: resourcechange
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.target.kind
      name: Target
      type: string
    - jsonPath: .spec.order
      name: Order
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceChange represents a single resource mutation belonging
          to a Transaction.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourceChangeSpec describes a single mutation within a transaction.
            properties:
              content:
                description: |-
                  Content holds the resource manifest or patch data.
                  Required for Create, Update, and Patch. Ignored for Delete.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              order:
                default: 0
                description: |-
                  Order controls execution sequence. Changes with the same order value
                  are independent and may execute in parallel in future versions.
                  Changes with lower order values execute first.
                format: int32
                type: integer
              target:
                description: Target identifies the resource to mutate.
                properties:
                  apiVersion:
                    description: APIVersion is the group/version of the target resource
                      (e.g. "v1", "apps/v1").
                    type: string
                  kind:
                    description: Kind is the resource kind (e.g. "ConfigMap", "Deployment").
                    type: string
                  name:
                    description: Name of the target resource.
                    type: string
                  namespace:
                    description: Namespace of the target resource. Defaults to the
                      Transaction's namespace if empty.
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              type:
                description: 'Type is the kind of mutation: Create, Update, Patch,
                  or Delete.'
                enum:
                - Create
                - Update
                - Patch
                - Delete
                type: string
            required:
            - target
            - type
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
