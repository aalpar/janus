---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: restores.backup.janus.io
spec:
  group: backup.janus.io
  names:
    kind: Restore
    listKind: RestoreList
    plural: restores
    singular: restore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.backupRef
      name: Backup
      type: string
    - jsonPath: .status.currentStep
      name: Step
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Restore is a request to restore a target resource from a Backup
          using the restore semantics defined in the associated BackupContract.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RestoreSpec defines a request to restore from a backup.
            properties:
              backupRef:
                description: backupRef is the name of the Backup resource to restore
                  from.
                type: string
              targetNamespace:
                description: |-
                  targetNamespace is the namespace to restore into.
                  If not specified, defaults to the original backup namespace.
                type: string
            required:
            - backupRef
            type: object
          status:
            description: RestoreStatus defines the observed state of a Restore.
            properties:
              completedAt:
                description: completedAt is the time the restore operation completed.
                format: date-time
                type: string
              conditions:
                description: conditions represent the current state of the Restore.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentStep:
                description: currentStep is the index of the currently executing restore
                  step (0-based).
                type: integer
              message:
                description: message provides human-readable details about the current
                  phase or failure.
                type: string
              phase:
                description: phase is the current phase of the restore operation.
                enum:
                - Pending
                - Restoring
                - Verifying
                - Completed
                - Failed
                type: string
              startedAt:
                description: startedAt is the time the restore operation started.
                format: date-time
                type: string
              steps:
                description: steps records the result of each completed restore step.
                items:
                  description: RestoreStepStatus records the result of a single restore
                    step.
                  properties:
                    message:
                      description: message provides human-readable details about this
                        step.
                      type: string
                    ready:
                      description: ready indicates whether the readiness check for
                        this step passed.
                      type: boolean
                    resourcesRestored:
                      description: resourcesRestored is the number of resources restored
                        in this step.
                      type: integer
                    selector:
                      description: selector identifies which resources were restored
                        in this step.
                      properties:
                        apiGroup:
                          description: apiGroup is the API group of the resource.
                            Empty string means core API group.
                          type: string
                        kind:
                          description: kind is the kind of the resource (e.g. "Secret",
                            "PersistentVolumeClaim").
                          type: string
                        selector:
                          additionalProperties:
                            type: string
                          description: |-
                            selector is a label selector to match resources of this kind.
                            The controller will substitute the target resource's name for empty values,
                            allowing contracts to be written generically.
                          type: object
                      required:
                      - kind
                      type: object
                  required:
                  - selector
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
